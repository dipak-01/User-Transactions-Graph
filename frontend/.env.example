# Copy to .env when running locally if you want to override the default API base URL.
# This is especially handy when the backend is deployed to Vercel under a different domain.

VITE_API_BASE_URL=https://your-backend.vercel.app
